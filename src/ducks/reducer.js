//--INITIAL STATE--

const initialState = {
  id: null
};

//ACTION CONSTANTS

//ACTION BUILDERS

//REDUCER
export default (state = initialState, action) => {
  const { type, payload } = action;
  switch (type) {
    default:
      return state;
  }
};
